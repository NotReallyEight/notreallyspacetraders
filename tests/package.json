{
  "name": "notreallyspacetraders-tests",
  "version": "0.0.1",
  "description": "Tests for NotReallySpacetraders API wrapper",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NotReallyEight",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
